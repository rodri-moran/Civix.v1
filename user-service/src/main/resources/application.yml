spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5434/userdb
    username: user
    password: user
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update
        jdbc:
          time_zone: America/Argentina/Buenos_Aires
server:
  port: 8081
jwt:
  secret: miClaveSuperUltraMegaSecretaClaveDeJWT1234567890